import React from "react";
import { AuthContainer, AuthTitle } from "../../styled";

function Signin() {
  return (
    <AuthContainer>
      <AuthTitle>Signin</AuthTitle>
    </AuthContainer>
  );
}

export default Signin;
