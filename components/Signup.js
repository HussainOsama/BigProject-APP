import React from "react";
import { Text } from "react-native";
import { AppView } from "../styled";

const Signup = () => {
  return (
    <AppView>
      <Text>Signup</Text>
    </AppView>
  );
};

export default Signup;
