import React from "react";
import { Text } from "react-native";
import { AppView } from "./styled";

const Home = () => {
  return (
    <AppView>
      <Text>Home</Text>
    </AppView>
  );
};

export default Home;
